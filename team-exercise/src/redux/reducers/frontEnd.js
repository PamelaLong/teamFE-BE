import frontEnd from "./frontEnd";

const initialState = [];

const FrontEnd = (state = initialState, action) => {
  switch (action.type) {
    case "FRONTEND":
      return 
    
    default:
      return state;
  }
};
export default FrontEnd;
